name: Build and Upload

on:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}

jobs:
  build-dist:
    name: Build and Upload
    uses: ./.github/workflows/verify-build.yml
    with:
      release: true
    secrets: inherit

  update-release-issue:
    needs: build-dist
    name: Update Release Issue
    uses: ./.github/workflows/update-release-issue.yml
    secrets: inherit
