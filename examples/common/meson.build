examples_common_inference_headers = files([
  'inference_helpers.h',
])

examples_common_inference_sources = files([
  'inference_helpers.c',
])

examples_common_mydata_headers = files([
  'mydata.h',
])

examples_common_mydata_sources = files([
  'mydata.c',
])
